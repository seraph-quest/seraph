# 09. Reference Systems And Evidence

## Verified Inputs For This Research Pass

- local `maas` repository structure under `/Users/bigcube/Desktop/repos/maas`
- Seraph’s current repo state under `backend/`, `frontend/`, `daemon/`, and `docs/`
- Seraph’s existing internal research and legacy roadmap material

## What Was Verified

- `maas` uses a clean split between numbered `docs/research/` files and numbered `docs/implementation/` files plus `STATUS.md`
- Seraph had multiple overlapping planning systems before this restructure
- Seraph’s current shipped surfaces include real observer, tool, runtime, and embodiment foundations that the new docs must reflect

## What Was Not Available Locally

The repositories named `hermes`, `ironclaw`, and `openclaw` were not present under `/Users/bigcube/Desktop/repos` during this analysis pass. This research tree therefore does not claim a direct current-code comparison against those repos.

The only `openclaw`-related input available locally inside Seraph was the legacy reference file `docs/docs/development/openclaw-feature-parity.md`.

## Implication

This research set is grounded in:

- direct inspection of Seraph
- direct inspection of `maas`
- explicit non-claims where other repos were unavailable

That keeps the new source-of-truth surface anchored to evidence instead of filling gaps with guesswork.
